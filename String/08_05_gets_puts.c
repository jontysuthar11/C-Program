#include<stdio.h>

int main()
{
    char s[40];
    printf("Enter your name :");
    // scanf("%s",s);
    gets(s);    // yeh input put krega 
    puts(s);   // yeh   print krega output


    // printf("the name u enter is %s",s);
    return 0;
}